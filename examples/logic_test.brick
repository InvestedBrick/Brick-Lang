#include stdlib/int_to_ascii.brick

brick main{
    
    dec x as int -> 0;
    for dec i as int -> 0; i < 10 and x < 5; i++;{
        output itoa(i), " " noend;
        x++;
    }
    output;
    output itoa(x);
    output itoa(x++);
    output itoa(x--);
    __asm__{"mov eax,eax"};
    exit 0;
}